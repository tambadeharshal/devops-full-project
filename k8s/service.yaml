apiVersion: v1
kind: Service

metadata:
  name: devops-calculator

spec:
  type: NodePort

  selector:
    app: devops-calculator

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080